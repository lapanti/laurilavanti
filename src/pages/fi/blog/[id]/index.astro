---
import { allMdxPosts } from '../../../../lib/mdxPosts'

export function getStaticPaths() {
    return allMdxPosts.filter((p) => p.lang === 'fi').map((p) => ({ params: { id: String(p.id) } }))
}

return Astro.redirect(
    `/fi/blog/${Astro.params.id}/${allMdxPosts.find((p) => p.lang === 'fi' && String(p.id) === Astro.params.id)!.slug}/`,
    301
)
---
