---
import { tags } from '../../content/tags'

export function getStaticPaths() {
    return tags.map((t) => ({ params: { tag: t.id } }))
}

return Astro.redirect(`/fi/category/${Astro.params.tag}/`, 301)
---
