---
import { allMdxPosts } from '../../../../lib/mdxPosts'

export function getStaticPaths() {
    return allMdxPosts.filter((p) => p.lang === 'en').map((p) => ({ params: { id: String(p.id) } }))
}

return Astro.redirect(
    `/en/blog/${Astro.params.id}/${allMdxPosts.find((p) => p.lang === 'en' && String(p.id) === Astro.params.id)!.slug}/`,
    301
)
---
