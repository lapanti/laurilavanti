---
import { getExcerptPosts } from '../lib/mdxPosts'
import { sizes } from '../lib/styles'
import Excerpt from './excerptList/Excerpt.astro'

interface Props {
    currentSlug?: string
    excludeIds?: number[]
    lang?: 'en' | 'fi' | 'sv'
    limit?: number
    onlyIds?: number[]
    relatedTags?: string[]
    tag?: string
}

const { currentSlug, excludeIds, lang = 'fi', limit, onlyIds, relatedTags, tag } = Astro.props

const nodes = getExcerptPosts({ currentSlug, excludeIds, lang, limit, onlyIds, relatedTags, tag })
---

<style
    define:vars={{
        sizes1: sizes[1],
        sizes2point5: sizes[2.5],
        sizes3point5: sizes[3.5],
        sizes4: sizes[4],
    }}
>
    ul {
        column-gap: var(--sizes4);
        display: grid;
        grid-column-start: 3;
        grid-template-columns: 1fr;
        grid-template-rows: auto;
        margin-top: var(--sizes1);
        row-gap: var(--sizes3point5);
    }

    @media (min-width: 1200px) {
        ul {
            grid-template-columns: repeat(3, 1fr);
            margin-top: var(--sizes2point5);
        }
    }
</style>
<ul>
    {
        nodes.map((node) => (
            <Excerpt
                alt={node.alt}
                date={node.publishDate}
                excerpt={node.description}
                image={node.heroImage}
                lang={lang}
                slug={node.url.replace(/^\//, '').replace(/\/$/, '')}
                tags={node.tags}
                title={node.title}
            />
        ))
    }
</ul>
