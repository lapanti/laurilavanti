---
import { sizes } from '../../../lib/styles'
---
<style define:vars={{ sizes05: sizes[0.5] }}>
    ul {
        list-style: none;
        position: relative;
    }

    ul > li:not(:first-child) {
        margin-top: var(--sizes05);
    }
</style>
<ul>
    <slot />
</ul>
