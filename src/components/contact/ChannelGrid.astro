---

---

<style>
    .channels {
        display: grid;
        gap: 1.5rem;
        grid-template-columns: 1fr;
        padding-top: 2.5rem;
    }

    @media (min-width: 769px) {
        .channels {
            grid-template-columns: repeat(2, minmax(0, 1fr));
        }
    }
</style>
<div class="channels">
    <slot />
</div>
