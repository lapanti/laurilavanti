---

---

<style>
    .channels {
        display: grid;
        gap: 24px;
        grid-template-columns: 1fr;
        padding-top: 40px;
    }

    @media (min-width: 769px) {
        .channels {
            grid-template-columns: repeat(2, minmax(0, 1fr));
        }
    }
</style>
<div class="channels">
    <slot />
</div>
