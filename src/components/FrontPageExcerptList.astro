---
import ExcerptList from './ExcerptList.astro'

interface Props {
    lang?: 'en' | 'fi' | 'sv'
    limit?: number
}

const { lang = 'fi', limit } = Astro.props
---

<ExcerptList lang={lang} limit={limit} />
