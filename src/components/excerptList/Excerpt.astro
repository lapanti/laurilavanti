---
import type { TagCollection, TagLink } from 'contentful'
import type { Image } from '../../types/contentful'

import { sizes } from '../../lib/styles'
import Meta from '../Meta.astro'
import Banner from './excerpt/Banner.astro'
import Description from './excerpt/Description.astro'

interface Props {
    allTags: TagCollection
    date?: string
    excerpt?: string
    image?: Image
    lang?: 'en' | 'fi' | 'sv'
    localAlt?: string
    localImage?: string
    slug?: string
    tags?: { sys: TagLink }[]
    title: string
}

const { allTags, date, excerpt, image, lang = 'fi', localAlt, localImage, slug, tags, title } = Astro.props

const metaAriaLabels = {
    en: (t: string) => `Meta information for post "${t}"`,
    fi: (t: string) => `Kirjoituksen "${t}" meta-tiedot`,
    sv: (t: string) => `Metainformation för inlägget "${t}"`,
}
---

<style define:vars={{ sizes35: sizes[35] }}>
    li {
        max-width: var(--sizes35);
    }
</style>
<li>
    <article aria-label={title} itemscope itemtype="https://schema.org/CreativeWork">
        <Banner image={image} localAlt={localAlt} localImage={localImage} slug={slug} title={title} />
        {date && tags && <Meta allTags={allTags} ariaLabel={metaAriaLabels[lang](title)} date={date} tags={tags} />}
        <Description excerpt={excerpt} />
    </article>
</li>
