---
interface Props {
    company?: string
    endYear?: number
    location?: string
    organization?: string
    school?: string
    startYear: number
}

const { company, endYear, location, organization, school, startYear } = Astro.props

const entity = organization || company || school || ''
const years = startYear === endYear ? `${startYear}` : `${startYear}–${endYear ?? ''}`
---

<span class="cv-org">
    {entity}
    {location ? ` (${location})` : ''} · {years}
</span>
